### Bug fixes

- A user guide now references `rubin-citation-copy.js` from the pages that carry a `Copy BibTeX` button instead of from every page of a site that merely declares citations: every page when the footer shows a citation, and otherwise only the pages that render a `citation-card`. A package guide that shows its citation on one card and keeps it out of the footer no longer loads the script on several hundred pages of API reference with no button to wire.
