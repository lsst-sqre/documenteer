### Other changes

- The shallow-clone warning from `documenteer.ext.lastmodified` can now be silenced with `suppress_warnings = ["documenteer.git_shallow"]` for builds where a shallow checkout is expected.
