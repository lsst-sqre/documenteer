### Other changes

- Bibliography entries with an `adsurl` field now end with an `[ADS]` link to their NASA ADS record. This is especially useful for conference proceedings, such as the ASP Conference Series, which often have no DOI and would otherwise carry no link at all.
