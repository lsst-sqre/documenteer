### Bug fixes

- Fixed the `remote-code-block` directive under Sphinx 9. Sphinx 9 stores a directive's file argument as a path, which collapses the `//` in a URL down to `/` (for example `https://host` becomes `https:/host`) and broke remote content fetching. The directive now preserves the original URL when retrieving content over HTTP.
