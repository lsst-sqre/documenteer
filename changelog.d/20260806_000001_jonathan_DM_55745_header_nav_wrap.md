### Bug fixes

- In the user-guide theme, the header's search field, light/dark switcher, and icon links no longer drop onto a second left-aligned line when a site has many top-level navigation links. That cluster now stays pinned to the right edge of the header, and the navigation links instead wrap onto additional lines within their own area. Mobile layouts (below the 960 px breakpoint) are unchanged.
