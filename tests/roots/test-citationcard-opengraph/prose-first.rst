Prose first
===========

A page that introduces itself before it shows its citation, with prose long
enough to fill the description sphinxext-opengraph harvests on its own, so
that the description this page publishes is the one it published before the
card text was excluded from the walk.

.. citation-card::
