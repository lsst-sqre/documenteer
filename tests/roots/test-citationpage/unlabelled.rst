Visit table
===========

A page that records no explicit target at all, so a claim naming an anchor it
does not carry has nothing to be pointed at instead.

Raw exposures
-------------

A heading, which is an anchor but not an explicit target.
