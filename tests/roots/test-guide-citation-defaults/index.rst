Citation defaults guide
=======================

A minimal page built with the full user-guide stack by a site whose citations
take their shared fields from ``[project.citation_defaults]``, and whose table
states no ``date``.
