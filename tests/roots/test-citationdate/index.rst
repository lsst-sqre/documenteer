Citation date test
==================

This project declares its citations in :file:`conf.py`; nothing on this page
displays them. The warning under test is emitted once per build, while the
environment is checked, so a page is needed only to give the build something
to read.
