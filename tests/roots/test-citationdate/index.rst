Citation date test
==================

This project declares its citations in :file:`conf.py`; nothing on this page
displays them. The warning under test is emitted once per build, as the
builder is initialized, so this page exists only because a Sphinx project
needs a document to build.
